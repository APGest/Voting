import { CandidateModelSummary } from './candidate-model-summary.model';

describe('CandidateModelSummary', () => {
  it('should create an instance', () => {
    expect(new CandidateModelSummary()).toBeTruthy();
  });
});
